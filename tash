[1mdiff --git a/src/core/DevLog.cpp b/src/core/DevLog.cpp[m
[1mindex 37a8079..0d6bb6f 100644[m
[1m--- a/src/core/DevLog.cpp[m
[1m+++ b/src/core/DevLog.cpp[m
[36m@@ -16,8 +16,14 @@[m [mvoid gLog(const char* fmt, ...) noexcept[m
     vsnprintf(buf, sizeof(buf), fmt, args);[m
     va_end(args);[m
 [m
[31m-    if (!s_silent)[m
[32m+[m[32m    if (!s_silent) {[m
         fputs(buf, stdout);[m
[32m+[m[32m        // Redirecting stdout to a file switches libc to full buffering, so[m
[32m+[m[32m        // without this a tail -f (or a hung/crashed process) can look like[m
[32m+[m[32m        // dozens of lines never happened when they're just sitting in the[m
[32m+[m[32m        // unflushed buffer. Log lines are rare enough that this costs nothing.[m
[32m+[m[32m        fflush(stdout);[m
[32m+[m[32m    }[m
 [m
     // Strip trailing newline for clean ImGui display[m
     int len = static_cast<int>(strlen(buf));[m
[1mdiff --git a/src/core/Game.h b/src/core/Game.h[m
[1mindex 60ec68f..d6a97fd 100644[m
[1m--- a/src/core/Game.h[m
[1m+++ b/src/core/Game.h[m
[36m@@ -52,10 +52,23 @@[m [mpublic:[m
     Game() = default;[m
     ~Game() = default;[m
 [m
[31m-    bool init(const std::string& title, int width, int height);[m
[32m+[m[32m    // hidden=true creates the SDL window with SDL_WINDOW_HIDDEN instead of[m
[32m+[m[32m    // maximized — used by the --watch-ai-test CLI path so a background dev[m
[32m+[m[32m    // smoke test never appears on screen or steals focus from anything else[m
[32m+[m[32m    // running.[m
[32m+[m[32m    bool init(const std::string& title, int width, int height, bool hidden = false);[m
     void run();[m
     void shutdown();[m
 [m
[32m+[m[32m    // Dev/test hook: skip the menu entirely and drop straight into a[m
[32m+[m[32m    // Watch-AI game with the given player count/map shape/size so AI-vs-AI[m
[32m+[m[32m    // behaviour can be verified from the gLog output alone (no UI[m
[32m+[m[32m    // interaction needed). Slots 0 and 1 are put on the same team so[m
[32m+[m[32m    // alliance behaviour gets exercised too. shape: 0=Hexagon, 1=JebusCross,[m
[32m+[m[32m    // 2=JebusCross3, 3=Ring. size: 0=Small..3=XLarge — see --watch-ai-test[m
[32m+[m[32m    // in main.cpp.[m
[32m+[m[32m    void autoStartWatchAI(int playerCount = 6, int shape = 0, int size = 0);[m
[32m+[m
 private:[m
     // ── Core loop ──────────────────────────────────────────────────────────────[m
     void processEvents();[m
[36m@@ -239,10 +252,19 @@[m [mprivate:[m
 [m
     // ── Economy / turn ────────────────────────────────────────────────────────[m
     Resources    m_playerResources;[m
[31m-    // Shared AI-team pool: the AI plays the same economy as the player (earns[m
[31m-    // income from its towns/mines, pays for units/buildings/heroes). Its only[m
[31m-    // allowed advantage is information (no fog of war).[m
[31m-    Resources    m_enemyResources;[m
[32m+[m[32m    // Per-AI-player economy: index i = ownerId (m_numHumanPlayers + 1 + i).[m
[32m+[m[32m    // Each bot earns income from and pays for its own towns/mines/units/heroes[m
[32m+[m[32m    // independently now — previously every AI player pooled into one shared[m
[32m+[m[32m    // "AI team" fund, which is also why they never fought each other (nothing[m
[32m+[m[32m    // about a shared pool respects individual ownership). AI's only allowed[m
[32m+[m[32m    // advantage over a human is information (no fog of war).[m
[32m+[m[32m    std::vector<Resources> m_aiResources;[m
[32m+[m[32m    Resources& aiResources(uint32_t ownerId) {[m
[32m+[m[32m        static Resources dummy;[m
[32m+[m[32m        int idx = static_cast<int>(ownerId) - m_numHumanPlayers - 1;[m
[32m+[m[32m        if (idx < 0 || idx >= (int)m_aiResources.size()) { dummy = Resources{}; return dummy; }[m
[32m+[m[32m        return m_aiResources[idx];[m
[32m+[m[32m    }[m
     TurnManager  m_turns;[m
 [m
     // AI-owned ids: towns/mines store enemy HERO ids, which are always above[m
[36m@@ -500,8 +522,28 @@[m [mprivate:[m
     bool        m_showGarrisonPanel   = false;[m
     int         m_garrisonSelSlot     = -1;[m
     int         m_garrisonSelSide     = -1;   // 0=hero army, 1=town garrison[m
[32m+[m[32m    bool        m_garrisonSplitMode   = false; // true if selection was ctrl+clicked (split-in-half on drop)[m
     void renderGarrisonPanel();[m
 [m
[32m+[m[32m    // Shared 7-slot unit-stack row UI (icon + count, click-to-select then[m
[32m+[m[32m    // click-target to move/merge/swap, ctrl+click to split in half) — used by[m
[32m+[m[32m    // both the garrison panel and the hero<->hero unit exchange so they read[m
[32m+[m[32m    // and behave identically. Only draws + tracks selection; it cannot perform[m
[32m+[m[32m    // the transfer itself since it only sees one army at a time. When the user[m
[32m+[m[32m    // clicks a target slot, it stores the click in m_slotTransferTarget* and[m
[32m+[m[32m    // leaves (selSide,selSlot) as the source — the caller resolves both arrays[m
[32m+[m[32m    // and calls resolveSlotTransfer() once after drawing every row for the frame.[m
[32m+[m[32m    int  m_slotTransferTargetSide = -1;[m
[32m+[m[32m    int  m_slotTransferTargetSlot = -1;[m
[32m+[m[32m    void drawUnitSlotRow(int side, std::vector<UnitStack>& army,[m
[32m+[m[32m                          int& selSide, int& selSlot, bool& splitMode);[m
[32m+[m[32m    // Call once after all rows for a screen are drawn. srcArmy/dstArmy are[m
[32m+[m[32m    // resolved by the caller from (selSide) and (m_slotTransferTargetSide).[m
[32m+[m[32m    // Performs move/merge/swap (or split-in-half if splitMode was set) and[m
[32m+[m[32m    // clears the shared selection/target state.[m
[32m+[m[32m    void resolveSlotTransfer(std::vector<UnitStack>& srcArmy, std::vector<UnitStack>& dstArmy,[m
[32m+[m[32m                              int& selSlot, bool& splitMode);[m
[32m+[m
     std::unordered_set<int> m_mageGuildT4BonusGiven;  // hero IDs that already got the T4 mana bonus this game[m
 [m
     // ── Town service overlay flags (opened via "Town Services" bar) ───────────[m
[36m@@ -549,10 +591,15 @@[m [mprivate:[m
     std::string m_lostTownName;[m
 [m
     // ── Unit exchange between player heroes ────────────────────────────────────[m
[32m+[m[32m    // Same slot-grid / click-select-then-click-target model as the garrison[m
[32m+[m[32m    // panel (0=hero A, 1=hero B), including ctrl+click-to-split.[m
     bool        m_showUnitExchange  = false;[m
     int         m_exchangeHeroIdx   = -1;   // index of the OTHER hero (in m_heroes)[m
[31m-    int         m_exchangeSelSlotA  = -1;   // selected slot in hero A's army[m
[31m-    int         m_exchangeSelSlotB  = -1;   // selected slot in hero B's army[m
[32m+[m[32m    int         m_exchangeSelSide   = -1;   // 0=hero A, 1=hero B[m
[32m+[m[32m    int         m_exchangeSelSlot   = -1;[m
[32m+[m[32m    bool        m_exchangeSplitMode = false;[m
[32m+[m[32m    int         m_exchangeSelArtifactSide = -1; // artifact trade: 0=hero A inventory, 1=hero B[m
[32m+[m[32m    int         m_exchangeSelArtifactIdx  = -1;[m
 [m
     // ── World object interactions ─────────────────────────────────────────────[m
     uint32_t m_pendingObjId          = 0;[m
[36m@@ -690,6 +737,7 @@[m [mprivate:[m
     // ── Main menu sub-state ───────────────────────────────────────────────────[m
     int  m_menuMode            = 0;   // 0=main, 1=newgame, 2=loadgame, 3=settings, 4=campaign, 5=battlesim[m
     int  m_newGameMapSize    = 0;   // 0=Small, 1=Medium, 2=Large, 3=XLarge[m
[32m+[m[32m    int  m_newGameMapShape   = 0;   // 0=Hexagon, 1=JebusCross, 2=JebusCross3, 3=Ring[m
     int  m_newGameFaction    = 0;   // 0=HolyOrder ... 8=Convergence[m
     int  m_newGameDifficulty = 1;   // 0=Easy, 1=Normal, 2=Hard[m
     int  m_newGameClassId    = 0;   // classId of chosen hero class (0=auto)[m
[36m@@ -697,10 +745,28 @@[m [mprivate:[m
     // ── HoMM-style game setup slots ───────────────────────────────────────────[m
     // Slot 0 is always you. Each additional slot is Human (hot-seat) or Bot,[m
     // with its own faction (9 = Random) and starting bonus.[m
[31m-    int m_setupPlayerCount = 2;                 // total players, 2..4 (map zones)[m
[31m-    int m_slotType[4]      = {0, 1, 1, 1};      // 0=Human, 1=Bot[m
[31m-    int m_slotFaction[4]   = {0, 9, 9, 9};      // 0..8 faction, 9=Random[m
[31m-    int m_slotBonus[4]     = {2, 2, 2, 2};      // 0=Artifact, 1=+5 faction resource, 2=+1500 gold[m
[32m+[m[32m    static constexpr int MAX_SETUP_SLOTS = 8;[m
[32m+[m[32m    int m_setupPlayerCount = 2;                             // total players, 2..8 (map zones)[m
[32m+[m[32m    int m_slotType[MAX_SETUP_SLOTS]    = {0, 1, 1, 1, 1, 1, 1, 1};  // 0=Human, 1=Bot[m
[32m+[m[32m    int m_slotFaction[MAX_SETUP_SLOTS] = {0, 9, 9, 9, 9, 9, 9, 9};  // 0..8 faction, 9=Random[m
[32m+[m[32m    int m_slotBonus[MAX_SETUP_SLOTS]   = {2, 2, 2, 2, 2, 2, 2, 2};  // 0=Artifact, 1=+5 faction resource, 2=+1500 gold[m
[32m+[m[32m    // 0 = free agent (default, everyone is their own team — current FFA[m
[32m+[m[32m    // behaviour). >0 = team number; slots sharing a team number are allies.[m
[32m+[m[32m    int m_slotTeam[MAX_SETUP_SLOTS]    = {0, 0, 0, 0, 0, 0, 0, 0};[m
[32m+[m
[32m+[m[32m    // ── Alliances ──────────────────────────────────────────────────────────[m
[32m+[m[32m    // Live-game team per player slot, index = ownerId - 1 (slot s -> ownerId[m
[32m+[m[32m    // s+1 for both human and AI slots — matches Town::ownerId/Hero::ownerId).[m
[32m+[m[32m    // Populated from m_slotTeam at game start; persisted across save/load.[m
[32m+[m[32m    std::vector<int> m_playerTeam;[m
[32m+[m[32m    bool isAllied(uint32_t a, uint32_t b) const {[m
[32m+[m[32m        if (a == b) return true;[m
[32m+[m[32m        int ia = static_cast<int>(a) - 1, ib = static_cast<int>(b) - 1;[m
[32m+[m[32m        if (ia < 0 || ia >= (int)m_playerTeam.size() || ib < 0 || ib >= (int)m_playerTeam.size())[m
[32m+[m[32m            return false;[m
[32m+[m[32m        int ta = m_playerTeam[ia], tb = m_playerTeam[ib];[m
[32m+[m[32m        return ta != 0 && ta == tb; // team 0 = free agent, allied with no one[m
[32m+[m[32m    }[m
 [m
     // ── Hotseat multiplayer state ─────────────────────────────────────────────[m
     int  m_numHumanPlayers      = 1;   // 1=singleplayer, N=N-player hotseat[m
[1mdiff --git a/src/core/Game_Combat.cpp b/src/core/Game_Combat.cpp[m
[1mindex 74f8363..66405a9 100644[m
[1m--- a/src/core/Game_Combat.cpp[m
[1m+++ b/src/core/Game_Combat.cpp[m
[36m@@ -1,2545 +1,2565 @@[m
[31m-#include "Game.h"[m
[31m-#include "../hero/SkillRegistry.h"[m
[31m-#include "../hero/HeroClass.h"[m
[31m-#include "../magic/SpellRegistry.h"[m
[31m-#include "../combat/DamageCalc.h"[m
[31m-#include <imgui.h>[m
[31m-#include <stdio.h>[m
[31m-#include <sstream>[m
[31m-#include <algorithm>[m
[31m-#include <cmath>[m
[31m-[m
[31m-// ── Helper: resolve (faction, tier) for a CombatUnit ─────────────────────────[m
[31m-static std::pair<int,int> unitFactionTier(const CombatUnit& u,[m
[31m-                                          const std::vector<UnitDef>& defs)[m
[31m-{[m
[31m-    if (u.defId != 0) {[m
[31m-        for (const auto& d : defs)[m
[31m-            if (d.id == u.defId)[m
[31m-                return { static_cast<int>(d.faction), d.tier };[m
[31m-    }[m
[31m-    if (u.factionHint >= 0 && u.factionHint < 9)[m
[31m-        return { u.factionHint, u.stackSlot + 1 };  // stackSlot = tier-1 set by ArmyBuilder[m
[31m-    return { -1, 1 };  // truly unknown — no sprite lookup[m
[31m-}[m
[31m-[m
[31m-// ── Helper: ranged-attack projectile tint per faction's magic/weapon identity ──[m
[31m-static ImU32 factionProjectileColor(int fid)[m
[31m-{[m
[31m-    switch (fid) {[m
[31m-        case 0: return IM_COL32(255, 230, 140, 255); // Holy Order — golden light[m
[31m-        case 1: return IM_COL32(220, 40,  40,  255); // Crimson Wardens — blood red[m
[31m-        case 2: return IM_COL32(120, 220, 90,  255); // Thornkin — nature green[m
[31m-        case 3: return IM_COL32(160, 90,  220, 255); // Eternal Empire — death purple[m
[31m-        case 4: return IM_COL32(200, 20,  20,  255); // Bloodsworn — dark blood red[m
[31m-        case 5: return IM_COL32(150, 60,  220, 255); // Voidkin — void violet[m
[31m-        case 6: return IM_COL32(255, 150, 40,  255); // Iron Assembly — forge orange[m
[31m-        case 7: return IM_COL32(160, 200, 60,  255); // Amalgamate — sickly green[m
[31m-        case 8: return IM_COL32(180, 220, 255, 255); // Convergence — prismatic cyan[m
[31m-        default: return IM_COL32(255, 225, 140, 255);[m
[31m-    }[m
[31m-}[m
[31m-[m
[31m-// ── Helper: resolve a siege engine unit's name to its m_engineTex index ──────[m
[31m-// Names must match exactly what makeSiegeEngines() assigns below. Order must[m
[31m-// match kEngineKeys in Game_Core.cpp's texture-loading loop.[m
[31m-static int engineTexIndex(const std::string& name)[m
[31m-{[m
[31m-    // Count must match Game::NUM_ENGINE_KEYS (kept as a literal here since[m
[31m-    // that constant is private and this is a free function, not a member).[m
[31m-    static const char* kNames[14] = {[m
[31m-        "Catapult", "Battering Ram", "Trebuchet", "Siege Tower",[m
[31m-        "Divine Trebuchet", "Silver Trebuchet", "Living Tower", "Bone Crusher",[m
[31m-        "Blood Catapult", "Void Caster", "Iron Ram", "Iron Catapult",[m
[31m-        "Iron Trebuchet", "Flesh Drill",[m
[31m-    };[m
[31m-    for (int i = 0; i < 14; ++i)[m
[31m-        if (name == kNames[i]) return i;[m
[31m-    return -1;[m
[31m-}[m
[31m-[m
[31m-// ── Helper: render-time readability pass for a unit sprite ──────────────────[m
[31m-// Several faction sheets bake partial alpha / sparse silhouettes into the art[m
[31m-// (see SPRITE_HANDOFF.md) which makes units blend into terrain. Rather than[m
[31m-// editing 50+ PNGs, fix it at draw time: a dark outer ring + light inner rim[m
[31m-// (readable against both light and dark terrain) drawn as offset copies of the[m
[31m-// sprite's own silhouette, then two extra full-alpha passes that compound the[m
[31m-// sprite's intrinsic per-pixel alpha (1-(1-a)^n) to solidify translucent art.[m
[31m-// Caller still draws the real (tinted) sprite on top afterwards.[m
[31m-static void drawUnitReadabilityPass(ImDrawList* dl, ImTextureID tid,[m
[31m-                                     const ImVec2& pMin, const ImVec2& pMax,[m
[31m-                                     const ImVec2& uv0, const ImVec2& uv1)[m
[31m-{[m
[31m-    static const float kOffX[8] = { 3.2f, -3.2f,  0.0f,  0.0f,  2.4f, -2.4f,  2.4f, -2.4f };[m
[31m-    static const float kOffY[8] = { 0.0f,  0.0f,  3.2f, -3.2f,  2.4f,  2.4f, -2.4f, -2.4f };[m
[31m-    const ImU32 kDarkOutline  = IM_COL32(8, 7, 10, 255);[m
[31m-    for (int i = 0; i < 8; ++i)[m
[31m-        dl->AddImage(tid, {pMin.x + kOffX[i], pMin.y + kOffY[i]},[m
[31m-                          {pMax.x + kOffX[i], pMax.y + kOffY[i]}, uv0, uv1, kDarkOutline);[m
[31m-[m
[31m-    const ImU32 kLightRim = IM_COL32(240, 236, 224, 210);[m
[31m-    for (int i = 0; i < 8; ++i)[m
[31m-        dl->AddImage(tid, {pMin.x + kOffX[i] * 0.5f, pMin.y + kOffY[i] * 0.5f},[m
[31m-                          {pMax.x + kOffX[i] * 0.5f, pMax.y + kOffY[i] * 0.5f}, uv0, uv1, kLightRim);[m
[31m-[m
[31m-    dl->AddImage(tid, pMin, pMax, uv0, uv1, IM_COL32(255, 255, 255, 255));[m
[31m-    dl->AddImage(tid, pMin, pMax, uv0, uv1, IM_COL32(255, 255, 255, 255));[m
[31m-}[m
[31m-[m
[31m-// ── Combat update ─────────────────────────────────────────────────────────────[m
[31m-void Game::updateCombat(float dt)[m
[31m-{[m
[31m-    const auto& mouse = m_input.mouse();[m
[31m-    float mx = static_cast<float>(mouse.x);[m
[31m-    float my = static_cast<float>(mouse.y);[m
[31m-[m
[31m-    if (mouse.leftDown && !ImGui::GetIO().WantCaptureMouse) {[m
[31m-        bool consumed = m_combatHUD.onMouseDown(mx, my);[m
[31m-        if (!consumed && m_combat.phase() == CombatPhase::PlayerTurn) {[m
[31m-            // Convert mouse → world → hex[m
[31m-            float wx = (mx - m_combatBoardOffX) / m_combatBoardScale;[m
[31m-            float wy = (my - m_combatBoardOffY) / m_combatBoardScale;[m
[31m-            HexCoord clicked = m_combat.grid().hexGrid().worldToHex(wx, wy);[m
[31m-[m
[31m-            if (m_combat.grid().inBounds(clicked)) {[m
[31m-                CombatUnit* active = m_combat.activeUnit();[m
[31m-                if (active && active->isPlayer) {[m
[31m-                    const CombatUnit* tgt = m_combat.grid().getUnitAt(clicked);[m
[31m-                    if (tgt && !tgt->isPlayer && tgt->alive) {[m
[31m-                        // Attack or shoot[m
[31m-                        CombatAction act;[m
[31m-                        act.type         = (active->shotsLeft > 0 && active->range > 0)[m
[31m-                                           ? ActionType::Shoot[m
[31m-                                           : ActionType::Attack;[m
[31m-                        act.targetUnitId = tgt->id;[m
[31m-                        act.target       = clicked;[m
[31m-                        m_combat.submitAction(act);[m
[31m-                    } else if (!tgt && m_combat.isSiege() &&[m
[31m-                               m_combat.grid().isWallTile(clicked)) {[m
[31m-                        // Siege: attack the wall / gate[m
[31m-                        m_combat.attackWall(clicked);[m
[31m-                    } else if (!tgt) {[m
[31m-                        // Move to empty reachable tile[m
[31m-                        auto reach = m_combat.grid().reachable([m
[31m-                            active->pos, active->speed, active->flying);[m
[31m-                        for (const auto& h : reach) {[m
[31m-                            if (h == clicked) {[m
[31m-                                CombatAction act;[m
[31m-                                act.type   = ActionType::Move;[m
[31m-                                act.target = clicked;[m
[31m-                                m_combat.submitAction(act);[m
[31m-                                break;[m
[31m-                            }[m
[31m-                        }[m
[31m-                    }[m
[31m-                }[m
[31m-            }[m
[31m-        }[m
[31m-    }[m
[31m-    // Right-click on a unit hex — show stat popup[m
[31m-    if (mouse.rightDown && !ImGui::GetIO().WantCaptureMouse) {[m
[31m-        float wx2 = (mx - m_combatBoardOffX) / m_combatBoardScale;[m
[31m-        float wy2 = (my - m_combatBoardOffY) / m_combatBoardScale;[m
[31m-        HexCoord rc = m_combat.grid().hexGrid().worldToHex(wx2, wy2);[m
[31m-        